我已完成对现有代码和数据库结构的分析，并制定了以下重构计划：

### 1. 前端重构 (`frontend/src/views/quality/targets/QualityTargets.vue`)

*   **新增/编辑弹窗升级**：
    *   新增“计算公式”输入域（多行文本）。
    *   新增“保证达标的措施”输入域（多行文本）。
    *   调整表单布局，使其更符合录入习惯。
*   **查询条件卡片优化**：
    *   保留“目标分类”（公司/部门）。
    *   新增“责任人”筛选。
    *   新增“统计频次”筛选。
*   **列表页调整**：
    *   确保列表展示所有关键字段，与 Excel 导出内容对应。

### 2. 后端重构 (`server/routes/quality-targets.js`)

*   **导出功能完全重写**：
    *   使用 `exceljs` 实现与图片完全一致的复杂表头样式（包含文件编号、版本号、页眉等）。
    *   **总目标区域**：自动查询数据库中“公司”类别的目标，拼接成“总目标”文本显示在表格顶部。
    *   **主表区域**：
        *   实现“部门名称”列的自动合并（即同一个部门的多个目标，部门名称只显示一次并合并单元格）。
        *   样式完全复刻：表头深蓝色背景、白字、边框、字体对齐等。
        *   包含所有列：部门名称、质量目标、计算公式、目标值、保证达标的措施、责任人、统计频次。
    *   **页脚区域**：添加“编制/日期”、“审核/日期”、“批准/日期”栏位。

### 3. 数据库确认
*   经检查，数据库表 `QualityTargets` 已包含 `CalculationFormula` 和 `Measures` 字段且长度足够，无需修改数据库结构。

确认后，我将立即开始执行代码修改。